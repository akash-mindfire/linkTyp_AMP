/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['daisyui.com', 'api.linktyp.com','uploads-ssl.webflow.com']
  }
}

module.exports = nextConfig